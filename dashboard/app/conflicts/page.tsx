import StubView from "@/components/StubView";

export default function ConflictsPage() {
  return (
    <StubView
      title="Conflicts"
      description="A filterable graph of every CONFLICTED_WITH edge from /conflicts/graph — e.g. every Medical vs. Logistics clash over a helicopter."
      phase="Wired in Phase 11"
    />
  );
}
