import StubView from "@/components/StubView";

export default function LedgerPage() {
  return (
    <StubView
      title="Ledger"
      description="Scrub through every tick's proposals, debate transcript, votes, escalations and vetoes from /sim/ledger."
      phase="Wired in Phase 11"
    />
  );
}
