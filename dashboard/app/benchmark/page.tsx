import StubView from "@/components/StubView";

export default function BenchmarkPage() {
  return (
    <StubView
      title="Benchmark"
      description="Society vs. single-agent bars with SD error bars across objectives-met, time, waste and token cost from /benchmark/compare."
      phase="Wired in Phase 12"
    />
  );
}
